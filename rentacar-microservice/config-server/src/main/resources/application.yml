spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        default-label: master
        git:
          uri: https://github.com/esraydin25/TurkcellGY-config-server
server:
  port: 8888
